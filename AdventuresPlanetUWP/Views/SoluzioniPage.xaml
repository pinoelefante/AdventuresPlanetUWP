<Page
    x:Class="AdventuresPlanetUWP.Views.SoluzioniPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AdventuresPlanetUWP.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:vm="using:AdventuresPlanetUWP.ViewModels"
    xmlns:UserControls="using:AdventuresPlanetUWP.Views.UserControls"
    xmlns:Converters="using:AdventuresPlanetUWP.Converters"
    xmlns:controls="using:Template10.Controls">
    <Page.DataContext>
        <vm:SoluzioniPageViewModel/>
    </Page.DataContext>
    <Page.Resources>
        <CollectionViewSource IsSourceGrouped="True" Source="{Binding ListaSoluzioni}" ItemsPath="Value" x:Key="SoluzioniList"/>
        <Converters:BoolToVisibility x:Key="BoolVisibility"/>
        <Converters:NotBoolToVisibility x:Key="NotBoolVisibility"/>
        <Converters:NotBoolean x:Key="NotBool"/>
    </Page.Resources>
    <Grid x:Name="LayoutRoot" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!--  page header  -->

        <controls:PageHeader BackButtonVisibility="Collapsed" Content="Soluzioni" Frame="{x:Bind Frame}" IsEnabled="False"/>

        <SemanticZoom Grid.Row="1">
            <SemanticZoom.ZoomedInView>
                <ListView ItemsSource="{Binding Source={StaticResource SoluzioniList}}">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <UserControls:UCSoluzione Tapped="openSoluzione" Holding="openFlyout" RightTapped="openFlyout">
                                <FlyoutBase.AttachedFlyout>
                                    <MenuFlyout>
                                        <MenuFlyoutItem Text="Aggiungi a preferiti" Click="AddPreferiti" Visibility="{Binding IsPreferita, Converter={StaticResource NotBoolVisibility}}"/>
                                        <MenuFlyoutItem Text="Rimuovi da preferiti" Click="DelPreferiti" Visibility="{Binding IsPreferita, Converter={StaticResource BoolVisibility}}"/>
                                        <MenuFlyoutItem Text="Condividi"/>
                                    </MenuFlyout>
                                </FlyoutBase.AttachedFlyout>
                            </UserControls:UCSoluzione>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                    <ListView.GroupStyle>
                        <GroupStyle HidesIfEmpty="True">
                            <GroupStyle.HeaderTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Key}" Foreground="{StaticResource SystemControlForegroundAccentBrush}" FontSize="18" FontWeight="Bold" Padding="4" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                                </DataTemplate>
                            </GroupStyle.HeaderTemplate>
                        </GroupStyle>
                    </ListView.GroupStyle>
                </ListView>
            </SemanticZoom.ZoomedInView>
            <SemanticZoom.ZoomedOutView>
                <GridView ItemsSource="{Binding Source={StaticResource SoluzioniList}, Path=CollectionGroups}" HorizontalAlignment="Center">
                    <GridView.ItemTemplate>
                        <DataTemplate>
                            <Border BorderThickness="3" Width="64" MaxWidth="64" MinWidth="64">
                                <TextBlock Padding="8" Text="{Binding Group.Key}" FontSize="40" FontWeight="Light" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{StaticResource SystemControlForegroundAccentBrush}" />
                            </Border>
                        </DataTemplate>
                    </GridView.ItemTemplate>
                </GridView>
            </SemanticZoom.ZoomedOutView>
        </SemanticZoom>
        

        <StackPanel Grid.Row="2">
            <ProgressBar IsIndeterminate="{Binding IsUpdatingSoluzioni}" IsEnabled="{Binding IsUpdatingSoluzioni}" Visibility="{Binding IsUpdatingSoluzioni, Converter={StaticResource BoolVisibility}}" HorizontalAlignment="Stretch"/>
            <CommandBar>
                <CommandBar.PrimaryCommands>
                    <AppBarButton Label="Cerca" Icon="Find" Click="{x:Bind VM.GoToSearch}"/>
                    <AppBarButton Label="Aggiorna" Icon="Refresh" Click="{x:Bind VM.AggiornaSoluzioni}" IsEnabled="{Binding IsUpdatingSoluzioni, Converter={StaticResource NotBool}}"/>
                </CommandBar.PrimaryCommands>
            </CommandBar>
        </StackPanel>
        
    </Grid>
</Page>
